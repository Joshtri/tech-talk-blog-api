export const sendEmail = (email, title, link) => {
  console.log(`Email sent to ${email}: ${title} - ${link}`);
  // Tambahkan logika untuk mengirim email
};

export const sendWhatsAppMessage = (phone, title, link) => {
  console.log(`WhatsApp message sent to ${phone}: ${title} - ${link}`);
  // Tambahkan logika untuk mengirim pesan WhatsApp
};
